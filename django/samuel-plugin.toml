# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "django"
version = "1.0.0"
kind = "skill"
summary = "Django 5+ framework guardrails, patterns, and best practices for AI-assisted development. Use when working with Django projects, or when the user mentions Django. Provides ORM patterns, views, templates, DRF, admin, and security guidelines."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["django"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "framework"
language = "python"
extensions = ".py"
