# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "rails"
version = "1.0.0"
kind = "skill"
summary = "Rails 7+ framework guardrails, patterns, and best practices for AI-assisted development. Use when working with Rails projects, or when the user mentions Ruby on Rails. Provides ActiveRecord, Hotwire/Turbo, Action Cable, and convention-over-configuration guidelines."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["rails"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "framework"
language = "ruby"
extensions = ".rb,.erb"
