# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "assembly-guide"
version = "1.0.0"
kind = "skill"
summary = "Assembly language guardrails, patterns, and best practices for AI-assisted development. Use when working with assembly files (.asm, .s, .S), or when the user mentions Assembly/x86/ARM. Provides calling convention guidelines, register usage patterns, and debugging techniques specific to this project's coding standards."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["assembly-guide"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "language"
language = "assembly"
extensions = ".asm,.s,.S"
