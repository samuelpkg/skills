# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "java-guide"
version = "1.0.0"
kind = "skill"
summary = "Java language guardrails, patterns, and best practices for AI-assisted development. Use when working with Java files (.java), pom.xml, build.gradle, or when the user mentions Java. Provides modern Java patterns (records, sealed classes, streams), testing standards, and Spring conventions specific to this project's coding standards."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["java-guide"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "language"
language = "java"
extensions = ".java"
