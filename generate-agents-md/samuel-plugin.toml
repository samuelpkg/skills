# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "generate-agents-md"
version = "1.0.0"
kind = "skill"
summary = "Cross-tool compatibility workflow. Use when generating AGENTS.md files for compatibility with other AI coding tools, or creating tool-specific instruction files from CLAUDE.md."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["generate-agents-md"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
