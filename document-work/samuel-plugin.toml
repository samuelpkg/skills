# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "document-work"
version = "1.0.0"
kind = "skill"
summary = "Work documentation and pattern capture workflow. Use after completing significant work to capture patterns, decisions, and learnings in project memory files for future reference."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["document-work"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
