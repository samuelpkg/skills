# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "create-skill"
version = "1.0.0"
kind = "skill"
summary = "Agent Skill creation workflow. Use when creating new reusable AI agent skills, scaffolding skill directories, or converting existing guides into the portable Agent Skills standard format."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["create-skill"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
