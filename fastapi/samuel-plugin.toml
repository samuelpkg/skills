# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "fastapi"
version = "1.0.0"
kind = "skill"
summary = "FastAPI framework guardrails, patterns, and best practices for AI-assisted development. Use when working with FastAPI projects, or when the user mentions FastAPI. Provides async patterns, Pydantic models, dependency injection, and OpenAPI guidelines."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["fastapi"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "framework"
language = "python"
extensions = ".py"
