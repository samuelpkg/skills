# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "spring-boot-java"
version = "1.0.0"
kind = "skill"
summary = "Spring Boot (Java) framework guardrails, patterns, and best practices. Use when working with Spring Boot Java projects, or when the user mentions Spring Boot. Provides JPA, Security, REST, WebFlux, and enterprise Java guidelines."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["spring-boot-java"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "framework"
language = "java"
extensions = ".java"
