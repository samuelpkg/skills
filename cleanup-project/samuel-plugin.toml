# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "cleanup-project"
version = "1.0.0"
kind = "skill"
summary = "Project cleanup and pruning workflow. Use when removing unused guides, cleaning up stale components, or streamlining an Samuel installation to match actual project needs."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["cleanup-project"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
