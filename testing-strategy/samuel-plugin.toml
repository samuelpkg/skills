# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "testing-strategy"
version = "1.0.0"
kind = "skill"
summary = "Test planning and coverage strategy workflow. Use when designing test suites, establishing coverage targets, planning TDD approaches, or assessing testing gaps in existing codebases."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["testing-strategy"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
