# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "sql-guide"
version = "1.0.0"
kind = "skill"
summary = "SQL language guardrails, patterns, and best practices for AI-assisted development. Use when working with SQL files (.sql), database migrations, or when the user mentions SQL/databases. Provides query optimization patterns, indexing guidelines, transaction management, and migration standards specific to this project's coding standards."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["sql-guide"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "language"
language = "sql"
extensions = ".sql"
