# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "cuda-guide"
version = "1.0.0"
kind = "skill"
summary = "CUDA/GPU computing guardrails, patterns, and best practices for AI-assisted development. Use when working with CUDA files (.cu, .cuh), or when the user mentions CUDA/GPU programming. Provides kernel design patterns, memory hierarchy guidelines, and occupancy optimization specific to this project's coding standards."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["cuda-guide"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "language"
language = "cuda"
extensions = ".cu,.cuh"
