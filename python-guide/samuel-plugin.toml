# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "python-guide"
version = "1.0.0"
kind = "skill"
summary = "Python guardrails, patterns, and best practices for AI-assisted development. Use when working with Python files (.py), requirements.txt, pyproject.toml, or when the user mentions Python. Provides type hint conventions, virtual environment standards, testing with pytest, and async patterns."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["python-guide"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "language"
language = "python"
extensions = ".py"
