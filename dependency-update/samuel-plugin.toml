# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "dependency-update"
version = "1.0.0"
kind = "skill"
summary = "Safe dependency update workflow. Use when upgrading packages, resolving vulnerability alerts, updating major versions, or auditing dependency health across project ecosystems."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["dependency-update"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
