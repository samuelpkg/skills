# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "create-rfd"
version = "1.0.0"
kind = "skill"
summary = "Request for Discussion (RFD) creation workflow. Use when exploring technical options, building consensus on architecture decisions, or documenting design rationale before implementation begins. Creates structured documents that capture problem context, evaluate alternatives, and preserve decision-making rationale."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["create-rfd"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
