# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "generate-tasks"
version = "1.0.0"
kind = "skill"
summary = "Task generation and breakdown workflow. Use when decomposing a PRD or complex feature into implementable subtasks with clear acceptance criteria and dependencies."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["generate-tasks"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "workflow"
