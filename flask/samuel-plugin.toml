# Generated by scripts/migrate-v1-skills. Hand-edit after migration.
name = "flask"
version = "1.0.0"
kind = "skill"
summary = "Flask framework guardrails, patterns, and best practices for AI-assisted development. Use when working with Flask projects, or when the user mentions Flask. Provides blueprint patterns, extensions, Jinja2 templates, and REST API guidelines."
license = "MIT"
authors = ["samuel"]

[samuel]
framework = "^2.0.0"
protocol  = "^1.0.0"

[provides]
skills = ["flask"]

[capabilities.filesystem]
read  = ["/workspace"]
write = []

[metadata]
category = "framework"
language = "python"
extensions = ".py"
